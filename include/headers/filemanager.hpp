#pragma once
#include <iostream>
#include <string>
#include <fstream>
#include <sstream>
class fileManager{
    public:
    static std::string loadSource(const std::string& path);
    private:
    
};